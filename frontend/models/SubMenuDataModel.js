class SubMenuModel {
  constructor( id, title, titleData, placeHolder, icon) {
    this.id = id;
    this.title = title;
    this.titleData = titleData;
    this.placeHolder = placeHolder;
    this.icon = icon;
  }
}

export default SubMenuModel;