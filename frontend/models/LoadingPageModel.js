class LoadingPage {
  constructor(id, message, loading, button, icon) {
    this.id = id;
    this.message = message;
    this.loading = loading;
    this.button = button;
    this.icon = icon;
    
  }
}

export default LoadingPage;